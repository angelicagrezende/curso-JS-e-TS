// String, number, indefined, null, boolean, symbol

const nome = 'Angélica'// string
const nome1 = "Angélica" // string
const nome2 = `Angélica` // string

const num1 = 10 // number
const num2 = 10.50 // number

let nomeAluno // undefined -> não aponta para local nenhuma na memória
let sobrenomeAluno = null //Nulo ->não aponta pra local nenhum na memória

const aprovado = false //Boolean = true, false (lógico)

let a = 2
const b = a
console.log(a, b) // 2, 2

a = 3
console.log(a, b) // 3, 2